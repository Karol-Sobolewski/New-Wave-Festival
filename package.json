{
  "dependencies": {
    "express": "4.17.1",
    "express-cors": "^0.0.3",
    "express-handlebars": "3.1.0",
    "node-sass": "^4.14.1"
  },
  "scripts": {
    "start": "nodemon server.js",
    "build": "cd client && yarn install && yarn build"
  }
}
